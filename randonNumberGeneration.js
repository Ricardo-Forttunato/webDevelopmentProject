var n = Math.random();  // Gera um número aleatório usando a função random do módulo Math.
n = n * 12 + 1; // Gera um número aleatório entre 1 e 12
n = Math.floor(n); // Arredonda para baixo o número gerado.
console.log(n);